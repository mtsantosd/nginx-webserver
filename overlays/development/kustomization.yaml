apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev-env
namePrefix: dev-
patches:
- path: patch.yaml
  target:
    kind: Deployment
    name: nginx-web
configMapGenerator:
- name: nginx-configmap
  files:
    - index.html
resources:
- ../../base/
commonlabels:
  env: dev
